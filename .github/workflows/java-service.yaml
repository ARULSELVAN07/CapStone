name: Java Service CI

on:
  push:
    paths:
      - "java-service/**"
      - ".github/workflows/java-service.yaml"
  pull_request:
    paths:
      - "java-service/**"
      - ".github/workflows/java-service.yaml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Build
        working-directory: java-service
        run: mvn clean package -DskipTests

      - name: Test
        working-directory: java-service
        run: mvn test